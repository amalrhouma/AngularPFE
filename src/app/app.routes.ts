import { Routes } from '@angular/router';
import { AddAppointmentComponent } from './add-appointment/add-appointment.component';

export const routes: Routes = [
  { path: 'admin', component: AddAppointmentComponent },
];
